# ---- Node / frontend ----
node_modules/
dist/
dist-ssr/
build/
.vite/
*.local
.env
.env.*
!.env.example

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

# ---- Python (offline ephemeris generator) ----
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Swiss Ephemeris raw data files (NOT committed — Moshier mode needs none;
# if .se1 files are ever used locally, keep them out of the repo)
*.se1
ephe/

# Course guidelines (local reference only, not tracked — same as gdl-papers)
course/
course/*

# Generated intermediate scratch (the FINAL transitions JSON IS committed —
# see data/ in AGENTS.md). Only ignore scratch/temp outputs here:
generator/scratch/
*.tmp.json
.superpowers/
